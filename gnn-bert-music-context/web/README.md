# Cadence Lab UI

Next.js 16 app for the GNN–BERT music-context explorer.

```bash
npm install
npm run dev    # http://127.0.0.1:43180
```

Reads `public/data/demo.json` (written by `python -m src.evaluate`). Live inference proxies to the FastAPI server on port 43181.
